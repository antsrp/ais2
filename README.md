# ais2

**Execution**

```
usage: main.py [-h] [-a {s,r}]

optional arguments:
  -h, --help  show this help message and exit
  -a {s,r}    choose an option of program to do: send email (s) or receive email (r)
```
